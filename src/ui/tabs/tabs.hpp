#pragma once

#include <imgui.h>

namespace tabs {
    void render_index();
    void render_collections();
    void render_discover();
    void render_radio();
    void render_status();
    void render_config();
} // namespace tabs
